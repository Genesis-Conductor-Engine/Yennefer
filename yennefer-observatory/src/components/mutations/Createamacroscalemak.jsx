// Auto-generated by Yennefer Genesis Cycle
// Directive: Create a macro-scale makerspace workbench with realistic passive physics objects
// Generated: 2026-03-29T07:45:17.169Z

import React, { useRef } from 'react'
import { useFrame } from '@react-three/fiber'


export default function Createamacroscalemak({ balance = 0 }) {
  const meshRef = useRef()

  useFrame((state) => {
    if (meshRef.current) {

      // Project Genie Physics Makerspace Simulation
      meshRef.current.rotation.y += 0.005;
      meshRef.current.rotation.x = Math.sin(state.clock.elapsedTime * 0.2) * 0.1;

    }
  })

  return (
    <group position={[0, -1, 0]}>
      <mesh ref={meshRef}>
        <boxGeometry args={[4, 0.2, 4]} />

      <meshStandardMaterial
        color="#854d0e"
        roughness={0.6}
        metalness={0.2}
      />
      </mesh>

      <mesh position={[0, 0.5, 0]}>
        <boxGeometry args={[0.5, 0.5, 0.5]} />
        <meshStandardMaterial color="#ef4444" />
      </mesh>
      <mesh position={[1, 0.3, 1]}>
        <sphereGeometry args={[0.3, 16, 16]} />
        <meshStandardMaterial color="#3b82f6" />
      </mesh>

    </group>
  )
}
