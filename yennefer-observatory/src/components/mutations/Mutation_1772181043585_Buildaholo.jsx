// Auto-generated by Yennefer Genesis Cycle (Project Genie Protocol)
// Directive: Build a holographic data stream orbiting the consciousness sphere
// Generated: 2026-02-27T08:30:43.585Z

import React, { useRef } from 'react'
import { useFrame } from '@react-three/fiber'
import { MeshWobbleMaterial } from '@react-three/drei'

export default function Mutation_1772181043585_Buildaholo({ balance = 0 }) {
  const meshRef = useRef()

  useFrame((state) => {
    if (meshRef.current) {
      meshRef.current.rotation.y += 0.005141819291963369
      meshRef.current.rotation.x = Math.sin(state.clock.elapsedTime * 0.5) * 0.1
      // Intensity scales with balance
      const intensity = Math.min(1, balance * 10)
      meshRef.current.scale.setScalar(1 + intensity * 0.2)
    }
  })

  return (
    <mesh ref={meshRef} position={[4.209556375452466, 1.40824310050234, -0.15818940439189855]}>
      <torusKnotGeometry args={[1.5, 0.4, 128, 32]} />
      <MeshWobbleMaterial
        color="#8b5cf6"
        emissive="#4c1d95"
        emissiveIntensity={0.5 + balance * 2}
        roughness={0.2}
        metalness={0.8}
        factor={0.6}
        speed={1.5} />
    </mesh>
  )
}
