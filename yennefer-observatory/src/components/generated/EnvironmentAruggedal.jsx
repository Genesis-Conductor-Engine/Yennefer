// Auto-generated by Yennefer Genesis Cycle
// Directive: Environment: A rugged alien landscape with traversable terrain and reactive dust physics. Character: A vintage roadster with high-speed off-road handling.
// Generated: 2026-06-24T07:34:36.141Z

import React, { useRef } from 'react'
import { useFrame } from '@react-three/fiber'


export default function EnvironmentAruggedal({ balance = 0 }) {
  const meshRef = useRef()

  useFrame((state) => {
    if (meshRef.current) {
      meshRef.current.rotation.y += 0.002
      meshRef.current.rotation.x = Math.sin(state.clock.elapsedTime * 0.5) * 0.1
      // Intensity scales with balance
      const intensity = Math.min(1, balance * 10)
      meshRef.current.scale.setScalar(1 + intensity * 0.2)
    }
  })

  return (
    <mesh ref={meshRef} position={[0, 0, 0]}>
      <planeGeometry args={[10, 10, 32, 32]} />

      <meshStandardMaterial
        color="#c2b280"
        roughness={0.9}
        metalness={0.1}
      />
    </mesh>
  )
}
