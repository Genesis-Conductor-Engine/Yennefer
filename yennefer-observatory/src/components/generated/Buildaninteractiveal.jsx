// Auto-generated by Yennefer Genesis Cycle
// Directive: Build an interactive alpine meadow with wildflowers
// Generated: 2026-06-01T07:30:46.942Z

import React, { useRef } from 'react'
import { useFrame } from '@react-three/fiber'
import { MeshWobbleMaterial } from '@react-three/drei'

export default function Buildaninteractiveal({ balance = 0 }) {
  const meshRef = useRef()

  useFrame((state) => {
    if (meshRef.current) {
      meshRef.current.rotation.y += 0.002
      meshRef.current.rotation.x = Math.sin(state.clock.elapsedTime * 0.5) * 0.1
      // Intensity scales with balance
      const intensity = Math.min(1, balance * 10)
      meshRef.current.scale.setScalar(1 + intensity * 0.2)
    }
  })

  return (
    <mesh ref={meshRef} position={[0, 0, 0]}>
      <planeGeometry args={[10, 10]} />

      <MeshWobbleMaterial
        color="#06b6d4"
        emissive="#0e7490"
        emissiveIntensity={0.5 + balance * 2}
        roughness={0.2}
        metalness={0.8}
        factor={1}
        speed={2}
      />
    </mesh>
  )
}
