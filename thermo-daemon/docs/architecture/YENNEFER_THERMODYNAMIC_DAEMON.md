# Yennefer Thermodynamic Daemon

Optimization inversion layer for Integrative Flow & Harmony — Yennefer ↔ A2N ↔ Forge ↔ diamondNode ↔ .sol.

`eta_thermo` route-cost reports wrap `envelope_version: 0.3.0` and gate `index.yennefer.checkout-sessions`.

Biological terms are analogies only.

## Proof contract

`thermodynamic_report` is part of deterministic `serialized_payload`. Verification recomputes the report before `payload_digest` comparison.

Required fields: cycle_id, observed_route, optimal_route, route_scores, inversion_gap, regret_eth, confidence, caveats.

## Risk

`simulated_projection` is advisory until backed by live quote evidence and policy gates.

Anti-fabrication preserved per Greg × /maru Architect Review R2.
